#!/usr/bin/env ruby

require 'turbine_rb'

if ARGV[0] == 'function'
  path_to_data_app = ARGV[1] || Dir.getwd
  require "#{path_to_data_app}/app"

  TurbineRb.serve
end

if ARGV[0] == 'process'
  require "#{Dir.getwd}/app"
  runner = TurbineRb.run
end
